<%= form_for ([@user, @review]) do |f| %>

	<%= label_tag('book[title]', "Choose Book for Review") %>
	<%= select_tag 'book[title]', options_from_collection_for_select(@books, "id", "title") %> <br>

	<%= f.label :comments, "Enter Comments" %>
	<%= f.text_area :comments %><br>
	<%= f.label :rating, "Enter Rating - 1 to 5(best)" %>
	<%= f.text_field :rating %><br>
	<%= f.submit %>
<% end %>